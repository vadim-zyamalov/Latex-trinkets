\documentclass[12pt, a4paper, oneside]{extreport}

%!TEX root = main.tex
\newcommand{\mybibliographyfilename}{diploma.bib}

\input{preamble/preamble_text.tex}
\input{preamble/preamble_images.tex}
\input{preamble/preamble_tables.tex}
\input{preamble/preamble_gost.tex}

%%%%%%%%% Свои команды %%%%%%%%%%%

\def \a{\alpha}
\def \b{\beta}

\DeclareMathOperator*{\argmax}{arg\,max}
\DeclareMathOperator*{\argmin}{arg\,min}
\DeclareRobustCommand{\bbone}{\text{\usefont{U}{bbold}{m}{n}1}}
\DeclareMathOperator{\EX}{\mathbb{E}}% expected value


\begin{document}
	
	\tableofcontents  % генерация содержания 
	
	\chapter*{Введение}
	% Добавляем введение в содержание
	\addcontentsline{toc}{chapter}{Введение}
	
	Вот так должно выглядеть и быть оформлено введение.
	
	\input{ch3.tex}
	
	\chapter*{Заключение}
	% Добавляем заключение в содержание
	\addcontentsline{toc}{chapter}{Заключение}
	
	Здесь должно располагаться заключение.
	
	Ниже приведён пример оформления списка литературы.
	
	\newpage
	\nocite{*}  %Чтобы в список литературы напечаталичь все источники из bib-файла
	
	% Если нам хочется, чтобы в списке литературы были не полуторные интервалы можно воспользоваться следующим приёмом: 
	\begingroup
	\setstretch{1}
	\addcontentsline{toc}{chapter}{Список литературы}
	\printbibliography[title = Список литературы]
	\endgroup
	
	
	%%%%%%%%%%%%%%%%%%%% Приложения %%%%%%%%%%%%%%%%%%%%
	
	\appendix
	\renewcommand{\thechapter}{\Asbuk{chapter}}
	
	%%%%%%%%%% titlesec для приложений
	\titleformat{\chapter}
	{\normalfont\bfseries\large}{\chaptertitlename~\thechapter}{0.25em}{\normalfont}
		
	\titlecontents{chapter}
	[0 em] % 
	{\normalsize}
	{\makebox[7em][l]{Приложение \thecontentslabel}}
	{Приложение }
	{\titlerule*[10pt]{.}\contentspage}
	
	
	\chapter[Какой-то код]{Какой-то код}\label{app-a}
	
	\begin{minted}[breaklines]{R}
	sma <- function(y, n){
	result <- list()
	result$y <- y
	result$n <- n
	y_sma <- c()
	for(i in (n+1):length(y)){
	y_sma <- c(y_sma, mean(y[(i-n+1):i]))
	}
	# результаты
	result$y_sma <- y_sma
	return(result)
	}
	\end{minted}
	
	Комментарий к коду.
	
\end{document}

\NeedsTeXFormat{LaTeX2e}[1994/06/01]
\ProvidesPackage{aufgaben}[2025/09/18 LaTeX package for easy tasks numbering]

\newif\if@taskonly
\@taskonlyfalse

\DeclareOption{taskonly}{\@taskonlytrue}
\ProcessOptions\relax

\RequirePackage{enumitem}
\RequirePackage{calc}

%%% Упражнения %%%

\newlist{tasks}{enumerate}{3}
\setlist[tasks,1]{%
	label={\textbf{№\arabic{chapter}.\arabic*:}},
	ref={\arabic{chapter}.\arabic*},
	align=left,
	labelindent=0pt,
	labelsep*=.25em,
	labelwidth=\widthof{№\ref{tasksi-\EnumitemId}:},
	itemindent=0pt,
	leftmargin=!,
	after={\label{tasksi-\EnumitemId}}
}
\setlist[tasks,2]{%
	label={\arabic*.},
	ref={(\arabic*)},
	align=left,
	labelindent=0pt,
	labelsep*=.25em,
	labelwidth=\widthof{\ref{tasksii-\EnumitemId}.},
	itemindent=0pt,
	leftmargin=!,
	after={\label{tasksii-\EnumitemId}}
}
\setlist[tasks,3]{%
	label={\asbuk*.},
	ref={(\asbuk*)},
	align=left,
	labelindent=0pt,
	labelsep*=.25em,
	labelwidth=\widthof{\ref{tasksiii-\EnumitemId}.},
	itemindent=0pt,
	leftmargin=!,
	after={\label{tasksiii-\EnumitemId}}
}

\if@taskonly
\else
	\newcommand*{\answer@text}[1][]{%
		Ответ%
		\ifthenelse{\equal{#1}{}}%
		{}%
		{ (#1)}: %
	}

	\newcommand*{\solution@text}[1][]{%
		Решение%
		\ifthenelse{\equal{#1}{}}%
		{}%
		{ (#1)}: %
	}

	\newcommand*{\answer}[1][]{%
		\if@noskipsec\leavevmode\fi
		\def\@svsechd{%
			\textbf{\answer@text[#1]}}%
		\@xsect{-1em}%
	}

	\newcommand*{\solution}[1][]{%
		\if@noskipsec\leavevmode\fi
		\def\@svsechd{%
			\textbf{\solution@text[#1]}}%
		\@xsect{-1em}%
	}
\fi

\endinput
